import { colors } from './colors';
import { fonts } from './fonts';
import { buttons } from './buttons';
import { forms } from './forms';
import { icons } from './icons';
import { shadowsTheme } from './shadowsTheme';
import { categoryTheme } from './categoryTheme';

export const theme = {
  colors: colors,
  fonts: fonts,
  buttons: buttons,
  forms: forms,
  icons: icons,
  shadows: shadowsTheme,
  category: categoryTheme,
  minWidth: '220px',
  // Add other theme properties here, such as breakpoints, spacing, etc.
};
