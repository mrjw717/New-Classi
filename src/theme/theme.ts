import { colors } from './colors';
import { fonts } from './fonts';
import { buttons } from './buttons';
import { forms } from './forms';
import { icons } from './icons';

export const theme = {
  colors: colors,
  fonts: fonts,
  buttons: buttons,
  forms: forms,
  icons: icons,
  // Add other theme properties here, such as breakpoints, spacing, etc.
};
